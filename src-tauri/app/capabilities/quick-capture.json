{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "quick-capture",
  "description": "Minimal capability for the Quick Capture overlay window — only enough to create a memo, emit the quick-captured event, and hide itself.",
  "windows": ["quick-capture"],
  "permissions": [
    "core:default"
  ]
}
